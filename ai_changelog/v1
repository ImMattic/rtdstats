RTD Stats build log v1
Date: 2026-06-17

Implemented so far:
- Added docker-compose.yml for local orchestration of TimescaleDB, backend, and frontend.
- Added .env.example with database, GTFS-RT feed, polling, alert, and CORS settings.
- Filled backend config, database, and requirements setup.
- Added backend ORM models for vehicle positions and trip updates.
- Added backend Pydantic schemas for realtime, historical, stats, routes, alerts, and export responses.
- Added backend services for GTFS-RT fetching, ingestion, and APScheduler-based polling.
- Expanded GTFS decoder with bytes-based helpers for vehicle positions and trip updates.
- Added backend API v1 routes for realtime, historical, routes, stats, and exports.
- Wired backend app startup with CORS, router registration, and scheduler lifecycle management.
- Added Alembic configuration, initial migration, and backend Dockerfile.
- Filled frontend package.json, tsconfig.json, Next.js config, Tailwind, and PostCSS setup.
- Added frontend shared types, API client, query hooks, and utility helpers.
- Added frontend UI components for navigation, loading, exports, vehicle dialogs, stop tables, and dashboard widgets.
- Added frontend map, dashboard, and historical pages.
- Added frontend Dockerfile.

Notes:
- The frontend uses Leaflet for the live vehicle map.
- The backend uses TimescaleDB hypertables for vehicle_positions and trip_updates.
- Google Maps traffic integration was deferred and left as a stub for later.
- Future change batches should be recorded as v2, v3, etc. in this folder.
