name: Build and Push Docker Images

on:
  push:
    branches:
      - main
      - staging

env:
  REGISTRY: docker.io

jobs:
  build-backend:
    name: Build & Push Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          USER=${{ secrets.DOCKERHUB_USERNAME }}
          SHA=${{ github.sha }}
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "tags=${USER}/rtdstats-backend:latest,${USER}/rtdstats-backend:${SHA}" >> "$GITHUB_OUTPUT"
          else
            echo "tags=${USER}/rtdstats-backend:staging,${USER}/rtdstats-backend:staging-${SHA}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-frontend:
    name: Build & Push Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          USER=${{ secrets.DOCKERHUB_USERNAME }}
          SHA=${{ github.sha }}
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "tags=${USER}/rtdstats-frontend:latest,${USER}/rtdstats-frontend:${SHA}" >> "$GITHUB_OUTPUT"
          else
            echo "tags=${USER}/rtdstats-frontend:staging,${USER}/rtdstats-frontend:staging-${SHA}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: frontend
          file: frontend/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
